package network.bisq.mobile.domain.utils

actual fun restartProcess(view: Any?) {
    println("restartApp is not available on iOS")
}

actual fun killProcess(view: Any?) {
    println("terminateApp is not available on iOS")
}